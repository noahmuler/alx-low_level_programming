#include <stdio.h>

/**
 * main - Entry Point
 *r
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char *text = "\"Programming is like building a multilingual puzzle\0";

	puts(text);
	return (0);
}
